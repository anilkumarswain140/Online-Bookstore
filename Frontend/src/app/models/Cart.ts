export interface Cart{
        productId: Number,
        quantity: Number,
        // name: string,
        // price: Number,
        total: Number,
        id : string,
        productimage : string;
        producttitle: string;
        rating : string;
        productprice : number
        subtotal : number
}